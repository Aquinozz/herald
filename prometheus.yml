global:
  scrape_interval: 10s

scrape_configs:
  - job_name: 'herald'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets:
          - 'gateway:8080'
          - 'endpoint-service:8081'
          - 'webhook-dispatcher:8082'