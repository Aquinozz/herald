global:
  scrape_interval: 10s

scrape_configs:
  - job_name: 'herald'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets:
          - 'localhost:8081'
          - 'localhost:8082'